---
description: Fetch a web page or X/Twitter post, analyze it, save to Obsidian, and recommend actionable uses
argument-hint: <url>
---

Invoke the digest skill with the provided URL argument. The URL can be any web page, article, blog post, or X/Twitter post link.

Read the digest skill at `${CLAUDE_PLUGIN_ROOT}/skills/digest/SKILL.md` and follow its instructions exactly, using `$ARGUMENTS` as the URL to process.

If no URL is provided in `$ARGUMENTS`, ask the user to provide a URL.
